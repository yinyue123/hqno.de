// VitePress looks for the theme beside the config it loaded, so a build from
// the repository root needs it here too. The theme itself stays with the pages.
export { default } from '../../docs/.vitepress/theme';
