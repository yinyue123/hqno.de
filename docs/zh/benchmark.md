# 给节点跑性能测试

重装一次，等半小时，你会拿到一个网址：一张说明这台机器到底是什么的页面 ——
CPU、硬盘、内存、IP 的信誉、到十几个地方的速度、回国的九条线路 ——
中文、英文、法文、德文，谁看它就用谁的语言。

它是拿来贴在你卖节点的地方的，或者直接发给问"我买的是啥"的人。

**页面上没有任何一项是被第三方核实过的。** 它写的是机器测出来的东西，加上你自己填的东西。
这句话两个方向都要记住：正因如此它对你的机器是诚实的，
也正因如此别人发给你的页面，值多少完全取决于你信他多少。

## 1. 地址

这一页要做的所有事，都从这一个镜像引用开始：

```
ghcr.io/yinyue123/nodequality
```

把一个容器重装到它上面，测试就会自己跑起来，只跑一次，在装完之后的第一次开机。
没有程序要装，没有命令要敲，没有第二步。

::: warning 你的容器需要一块 `/data` 盘
这次运行留下的所有东西都在那儿，包括那把之后能改这张页面的唯一钥匙，
而 `/data` 是重装唯一不抹掉的地方。没有它你只能发布一次，之后永远改不了。
容器页面上会写你有没有 —— 你马上要按的那个按钮，上面写的就是**保留 /data**。
:::

## 2. 在容器页面上重装

容器页面上的 **重装…** 会打开一个对话框，三栏。能填地址的是"我自己的镜像"这一栏：

<FigScreen :tabs="['我自己的镜像', '镜像市场', '这台主机上的归档']" :lines="[
  [{ t: '会清空 /。/data 是另一块盘，会保留。', tone: 'mute' }],
  ['镜像引用', { f: 'ghcr.io/yinyue123/nodequality', fw: 300 }],
  [{ t: '下载的量算在这个容器的流量里 —— 42 MB。', tone: 'mute', face: 'small' }],
  ['输入 bench 以确认', { f: 'bench' }],
  { align: 'right', cols: [{ b: '取消' }, { b: '重装，保留 /data' }] },
]" />

把地址贴进去，在下面那一格里输入这个容器自己的名字，按 **重装，保留 /data**。
大约一分钟后容器就回来了，跑在这个测试镜像上，测试已经开始了。

这一栏要出现，有两个前提：机器的策略得允许租户自带镜像，
容器得有一块自己的盘。缺哪一个，对话框会说；两个都是你的主机方能改的。

### 或者在容器里开个 shell

`reinstall` 是你**现在**这个系统自带的命令，所以下面这条就是同一件事，
从容器现在的样子里敲出来：

```
reinstall ref ghcr.io/yinyue123/nodequality
```

重建过程中你的 SSH 会断。那是重装本身，不是出错。

::: tip 为什么是重装，而不是"装个程序"
镜像本身**就是**那个程序。检测需要的每一个工具都已经编好放在里面了，
所以整个过程只下载测速用的数据，别的什么都不下 ——
不会有包管理器在磁盘测试跑到一半的时候插进来，把磁盘成绩搅乱。
:::

::: warning 回去要按按钮，不是敲命令
这个镜像没有带 `reinstall` —— 它是个跑分环境，不是通用系统。
装过去之后想回 Debian 或 Alpine，是容器页面上的 **重装** 按钮，走"镜像市场"那一栏。
东西没卡住，只是没法再从容器里面操作了。
:::

## 3. 等着，想看就看

**二十到五十分钟**，会走掉**几个 GB**的测速流量，算在你容器的额度里。
小套餐先看一眼额度还剩多少。

日志的最后一行就是你的网址，之后任何时候 `nq-shop show` 也会把它打出来：

```
tail -f /data/nodequality/first-boot.log
```
```
published as https://shop.hqno.de/r/xxxxxxxxxx
```

整个测试**只跑一次**。判断依据是有没有已发布的页面，
所以跑到一半挂掉的话，下次开机会重来；已经成功的则永远不会重跑 ——
重启容器不会让它再测一遍。

## 4. 页面上属于你的那一半

<FigRows :head="['页面上的', '来自']" :rows="[
  [{ t: 'CPU、内存、硬盘、IP 信誉、速度、线路', tone: 'mute' }, { t: '机器 —— 测出来的，改不了', tone: 'strong' }],
  [{ t: '这个节点叫什么', tone: 'mute' }, { t: '你', tone: 'accent' }],
  [{ t: '有哪些套餐，各卖多少钱', tone: 'mute' }, { t: '你', tone: 'accent' }],
  [{ t: '别人怎么找到你来买', tone: 'mute' }, { t: '你', tone: 'accent' }],
]" />

你的那一半是一个文件，第一次跑完它就已经在那儿了：

```
/data/nodequality/shop.json
```

用什么改都行 —— 容器里的 `vi`，或者从自己电脑走 SFTP。
它里面已经填好了一份写全的示例，所以下面这个结构你打开就能看到；
需要知道的是这几点。

**一个套餐，和它的价格。** 价格要写成**数字**，不是字符串。写 `45`，不要写 `"45"` ——
年付省多少是从这两个数算出来的，`450` 对上 12×45，四种语言都会自己印出"省 17%"。

```json
{
  "shop": {
    "title": { "zh": "洛杉矶 CN2 GIA", "en": "Los Angeles CN2 GIA" },
    "plans": [
      {
        "name": { "zh": "标准 · 1C / 1 GB", "en": "Standard · 1C / 1 GB" },
        "currency": "¥", "monthly": 45, "yearly": 450,
        "rows": [
          { "k": "CPU", "v": "1 vCPU" },
          { "k": "@k_ram", "v": "1 GB" },
          { "k": "@k_traffic", "v": "100 GB" }
        ]
      }
    ],
    "contacts": [ { "k": "@k_tg", "v": "@@yourname" } ]
  }
}
```

**四种语言，你只写一份。** 页面能渲染成 zh · en · fr · de，由读者挑。
你写的文字分三类，行为各不相同，而这个区别就是整件事的窍门：

| 你文件里写的 | 渲染出来 | 什么时候用 |
|---|---|---|
| `"@k_ram"` | 内存 / Memory / Mémoire / Speicher | 任何有标准叫法的东西 |
| `{"zh": "…", "en": "…"}` | 你自己的措辞，一种语言一份 | 表里不可能知道的东西 |
| `"1 GB"` | 到哪儿都是 `1 GB` | 数字、单位、AS 名 |

所以一行只写一次，法国人看到的就是 **Mémoire — 1 GB**，你一个法文字都不用写。
只有你才叫得出名字的部分 —— 机器叫什么、套餐叫什么 —— 用对象那种写法；
缺的语言会退回英文，再退回中文，再退回有什么用什么。

两个会咬人的细节：

- **`@` 是查表的起手式。** Telegram 账号写成 `"@yourname"` 会被当成键去查，
  出来变成 `yourname`，那个符号没了。要写 `"@@yourname"`，两个 `@` 表示一个字面的 `@`。
- **查不到的键**会把自己去掉 `@` 印出来，这正好用来抓错字：
  页面上如果写着 `k_rma`，那你本来想写的是 `@k_ram`。

## 5. 把改动发上去，还是同一个网址

两条命令，都很快，**都不会重跑测试**：

```
nq-shop build      # 把你改的折回页面里
nq-shop publish    # 覆盖掉已发布的那张
```

网址不变。`/data` 里那把钥匙就是"这张页面归你改"的凭据，
所以第二次发布是盖在第一张上，而不是又开一张新的。

<FigRows :head="['你做了什么', '代价', '网址']" :rows="[
  [{ t: '改了价格或名字', tone: 'mute' }, { t: '几秒 —— build、publish', tone: 'ok' }, { t: '不变', tone: 'ok' }],
  [{ t: '想让机器重新测一遍', tone: 'mute' }, { t: 'nq-shop run，20–50 分钟，几个 GB', tone: 'mute' }, { t: '不变', tone: 'ok' }],
  [{ t: '把 /data/nodequality/credentials.json 弄丢了', tone: 'mute' }, { t: '没有任何东西能替换那张页面', tone: 'bad' }, { t: '下次是新的', tone: 'bad' }],
]" />

::: warning 钥匙就是页面本身
`/data/nodequality/credentials.json` 是唯一能替换你这张页面的东西。
丢了的话，页面会一直挂在那儿、一直能看、永远改不动。
它能扛过重装，因为它在 `/data` 里；扛不过删容器。
页面重要的话，把它备份出来。
:::

想指定语言就带上查询参数，不带就让浏览器自己决定：

```
https://shop.hqno.de/r/xxxxxxxxxx?lang=en
```

## 6. 只重跑其中一部分

每一步都是独立的命令，`--skip` 接一串逗号分隔的名字，
可选 `ip`、`hw`、`net`、`bench`、`route`：

| | |
|---|---|
| `nq-shop bench` | 只跑 CPU 跑分，两分钟左右 |
| `nq-shop route` | 只跑回国的九条线路 |
| `nq-shop collect --skip net` | 除了那几个 GB 之外的全套 |
| `nq-shop build` | 用磁盘上现有的数据重建页面 |
| `nq-shop publish` | 推上去，还是同一个网址 |

::: warning `hqnode exec` 六十秒就放弃
如果你是从**宿主机**这边驱动，而不是在容器里开 shell，
`hqnode exec` 会在一分钟后返回 `context deadline exceeded`，还把锅甩给 agent。
命令本身在容器里照样跑，你丢的只是输出。
长命令要么用 detached 方式启动，要么干脆在容器里开个 shell 跑 —— 那边没有这个限制。
:::

## 7. 这张页面不会告诉你的事

- **上面没有一个分数是读者能自己核实的。** CPU 那几块来自 sysbench、7-Zip、
  OpenSSL 和 stress-ng，没有一个会把结果发布到任何公开的地方。
  它们跟另一张同样方式生成的页面比是诚实的，跟别的东西比则毫无意义。
- **多线程那个分数是偏乐观的。** 它取的是 cgroup 上限，
  而在 hqnode 上那是你的突发额度，不是卖给你的核数。
- **自由文本保持采集时的语言。** AS 名、城市名不会翻译；
  固定词表 —— 是/否、通/不通、风险等级 —— 会翻。
- **线路那两节要求路由真的能追踪。** 容器里是能追的，但只能走 UDP；
  你的宿主机如果把 UDP 过滤掉了，这两节会是空的，而不是错的。

## 接下来

- [使用你的容器](using-your-container.md) —— 重装到底做了什么，
  以及为什么 `/data` 是活下来的那部分
- [公网端口](ports.md) —— 如果你想把这张页面挂到自己的域名上
