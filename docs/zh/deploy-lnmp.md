# 部署 LNMP 网站

**这一页还是个占位 —— 步骤还没写。**

写好之后会讲：nginx、MySQL、PHP-FPM 装在一个容器里 —— 现成的 PHP 程序基本都是按
这套东西写的 —— 再在前面挂一个域名。

在那之前：[使用你的容器](/zh/using-your-container) 是打底的那一页，[添加你自己的软件](/app-setup-sources)
讲镜像里没带的东西怎么装。
