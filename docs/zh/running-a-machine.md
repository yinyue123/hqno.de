---
title: 自己跑一台机器
---

# 自己跑一台机器

你租一台服务器，把它切成容器，再转卖出去 —— 这一页讲的就是这件事，
从一台空机器，一直带到「一份容器已经在别人手里」。
自己的硬件走法完全一样；租来的是更常见的情况，所以这一页按它来写。

这件事的另一面 —— 你的租户看到什么、要做什么 —— 是[快速上手](quick-start.md)。读一遍那一页，
是最快看懂自己在卖什么的办法。

---

## 1. 什么必须从外面进得来

机器是自己往面板拨号并保持连着的，所以**面板完全不需要进得来**。但你的租户需要：

<FigRows :arrow="0" :head="['从公网进来', '到这台机器']" :rows="[
  [{ m: 'port 80, 443' }, '租户的网站'],
  [{ m: 'port 22' }, '租户的登录'],
  [{ t: '什么都不用', tone: 'mute' }, { t: '面板从不主动连进来 —— 是机器往外拨并保持在线', tone: 'mute' }],
]" />

租来的服务器本身就有公网地址，没有什么要转发的，中间也没有你自己的路由器。
真正挡在前面的通常是云厂商的防火墙 —— 安全组、网络规则，各家叫法不同 ——
而新开的机器往往只放行 22。开卖之前先把 80 和 443 放开，
否则租户的域名解析得好好的，就是没有应答。

其它要求很短：

<FigRows :rows="[
  ['一个不太老的 Linux', { t: '机器上的管理员权限', tone: 'mute' }],
  ['能往外联网', { t: '不需要固定地址', tone: 'mute' }],
]" />

容器要用到的那些配套软件，机器每次启动都会自己装，装得怎么样，面板上那台机器的页面会告诉你。

---

## 2. 没有什么要打开的

这里没有单独的「主机方账号」，也不用申请。任何账号都能接一台机器，同一个登录还能同时持有别人
给你的容器：

<FigRows :arrow="0" :rows="[
  [{ t: '你这一个账号', tone: 'strong' }, '你自己跑的机器'],
  [null, '别人给你的容器'],
]" />

所以这一步整个内容就是：登录，打开 **机器**。

---

## 3. 把机器接进来

**机器 → 添加一台机器**，两步。

<FigScreen title="添加机器 · 第 1 / 2 步" :lines="[
  [{ t: '在那台机器上用 root 执行：', tone: 'mute' }],
  [{ m: 'curl -fsSL http://panel.example.com/agent | sh' }],
  { align: 'right', cols: [{ b: '我装好了' }] },
]" />

<FigScreen title="添加机器 · 第 2 / 2 步" :lines="[
  [{ t: '然后把这个粘贴到同一台机器里：', tone: 'mute' }],
  [{ m: 'hqnode enroll --panel http://panel.example.com' }],
  [{ m: '              --code A1B2-C3D4' }],
  [{ t: '一次性 · 15 分钟内有效', face: 'small', tone: 'mute' }],
  { align: 'right', cols: [{ t: '等待 agent 上线 ⟳', face: 'small', tone: 'accent' }] },
]" />

两条命令里你面板自己的地址都是页面填好的，复制就行，不用手打。如果你的面板有多个地址能访问，
页面会问**那台机器**该用哪一个，并且先从你的浏览器测一遍每个地址的快慢 —— 你走的路不一定就是
机器走的路。

**你应该看到**它自己出现，就在第二条命令跑完的几秒钟内：

<FigScreen title="机器" :lines="[
  [{ t: 'hk-1', tone: 'strong' }, { t: '在线', tone: 'ok' }, '8 核 · 32 GB · 1.8 TB'],
  ['', '', { t: '0 个容器 · CPU 3% · 内存 11%', face: 'small', tone: 'mute' }],
]" />

两件事现在知道比以后知道好：

<FigRows :arrow="0" :rows="[
  ['租户连的那个地址', '在接进来的时候就定下了'],
  ['以后解绑', '面板忘掉它；机器上照旧在跑'],
]" />

---

## 4. 设置逐项看

在那台机器的页面上：

<FigScreen title="主机策略" :lines="[
  ['默认 vCPU', { f: '50 %', note: '一个核的百分比' }],
  ['默认内存', { f: '1024 MB' }],
  ['默认 swap', { f: '512 MB' }],
  ['默认磁盘', { f: '20480 MB' }],
  ['默认流量 / 月', { f: '500 GB' }],
  ['流量重置日', { f: '1' }],
  [{ k: '允许用户自带镜像', on: true }],
  { align: 'right', cols: [{ b: '保存主机设置' }] },
]" />

<FigRows :arrow="0" :rows="[
  [{ t: '那几个默认值', tone: 'strong' }, '只是预填到新建容器的表单里，'],
  [null, { t: '已经发出去的容器一个都不动', tone: 'mute' }],
  [{ t: '重置日', tone: 'strong' }, '这台机器上每个容器重新开始'],
  [null, { t: '计流量的那一天', tone: 'mute' }],
  [{ t: '自带镜像', tone: 'strong' }, '租户可以用自己的镜像重装；'],
  [null, { t: '代价是每次都要下载一遍，算他自己的流量', tone: 'mute' }],
]" />

### 两个数字就是这门生意的全部

<FigRows :arrow="0" :rows="[
  [{ t: 'vCPU', tone: 'strong' }, { t: '卖出 ½ 核' }, { t: '机器闲的时候跑得更快，', tone: 'mute' }],
  [null, null, { t: '忙起来回到 ½ —— 是地板', tone: 'mute' }],
  [{ t: '内存', tone: 'strong' }, { t: '卖出 1 GB' }, { t: '再要就被杀掉 —— 是墙', tone: 'mute' }],
]" />

定价之前先读懂这两行。CPU 份额是下限保证、不是上限封顶 —— 所以同一份容器在夜里空闲时跑分能快
一倍，而「这份到底多快」的诚实答案是忙时那个。内存没有这种好脾气：到线上，那份容器里最大的
进程会被杀掉。

一台机器能塞几份，你不用猜。塞不下的时候机器会拒绝创建，并且用一句话说清原因；页面上那行话会
告诉你风往哪边吹：

<FigRows :rows="[
  [{ t: '●', tone: 'bad' }, { t: '租户在排队 —— 这台主机已经把自己有的都卖出去了。' }],
  [{ t: '●', tone: 'ok' }, { t: '还算宽松：还有余量可卖。' }],
]" />

### 页面上其余的部分，各一行

<FigRows :arrow="0" :rows="[
  [{ t: '端口', tone: 'strong' }, '把网页或登录的门挪到别处'],
  [{ t: '存储', tone: 'strong' }, '容器的文件放在哪'],
  [{ t: '高级', tone: 'strong' }, '缺什么，以及一个替你装上的按钮'],
  [{ t: '镜像', tone: 'strong' }, '在机器上备好一个系统，创建容器就只要几秒'],
  [null, { t: '而且不下载任何东西', tone: 'mute' }],
]" />

有一项值得早点打开，在**高级**里：不装它的话，容器里的 `free` 和 `top` 显示的是整台机器的内存
而不是它自己的。限额两种情况下都完全按售出的执行 —— 变的只是数字 —— 但租户看的就是那些数字，
然后就来找你。

---

## 5. 创建一份容器

<FigScreen title="新建容器" :lines="[
  ['放在哪', { r: 'hk-1', on: true }],
  { cols: ['给谁用', { r: '我自己的' }, { r: '给某个用户', on: true }, { f: 'ana' }] },
  ['容器名', { f: 'wp-1' }],
  { cols: ['shell 登录名', { f: 'u7k2m9p' }, { f: '•••••••••' }] },
  ['镜像', { f: 'Alpine 3.24 ▾', note: '来自这台主机' }],
  ['限额', { t: '1 核 · 2 GB · 20 GB · 500 GB' }],
  ['到期', { t: '永不 —— 或者挑一个日期', tone: 'mute' }],
  ['域名', { t: '（可选）', tone: 'mute' }],
  { align: 'right', cols: [{ b: '创建' }] },
]" />

**给谁用**是决定后面走向的那个选择：

<FigRows :arrow="0" :rows="[
  [{ t: '我自己的', tone: 'strong' }, '不挂在任何人名下。登录信息给你，'],
  [null, { t: '你想什么时候交出去都行', tone: 'mute' }],
  [{ t: '给某个用户', tone: 'strong' }, '创建的同时就交出去，交给你填的那个用户名。'],
  [null, { t: '他在自己页面上改登录，你之后再也看不到', tone: 'mute' }],
]" />

选一个机器上已经有的镜像，创建就不下载任何东西、只要几秒 —— 机器上存的那一份，会被所有用它建
出来的容器共用。

**你应该看到：**

<FigScreen title="wp-1 正在运行" :lines="[
  [{ m: 'ssh u7k2m9p@hk-1.example.com' }],
  { cols: ['密码', { t: '8Kd2-vQx7-mR', face: 'mono' }, { t: '只显示一次', face: 'small', tone: 'bad' }] },
  [{ t: 'ana 持有这一份', tone: 'mute' }],
]" />

只显示一次就是只显示一次：面板不留着它。丢了就在那份容器的页面上重设。

---

## 6. 交出去

<FigRows :arrow="0" :rows="[
  [{ t: '我自己的', tone: 'strong' }, '登录信息先在你手上，之后按「把它交出去」'],
  [{ t: '给某个用户', tone: 'strong' }, '创建时就交出去了'],
  [null, { t: '两种都一样：登录被重写，你手上那份进不去', face: 'small', tone: 'mute' }],
]" />

**把它交出去**会给你一个链接和一串码，发哪个都行：

<FigScreen title="把它交出去" :lines="[
  [{ m: 'https://hqno.de/redeem?code=HQ-7F3K-2M9P' }],
  { cols: ['或者只发这串码：', { m: 'HQ-7F3K-2M9P' }] },
  [{ t: '14 天后过期', face: 'small', tone: 'mute' }],
]" />

在对方领取之前，什么都不会变；一串码过期了，随时可以从那份容器的页面上再生成一串。他一领取，
shell 登录立刻被重写 —— 你手上那份就用不了了，这正是它该有的样子。

然后把[快速上手](quick-start.md)发给他。那一页正好从你这串分享码停下的地方开始。

---

## 7. 长期跑下去

<FigRows :arrow="0" :rows="[
  ['改限额或到期日', '对正在跑的那份立刻生效'],
  ['暂停', '停下、留着、可恢复'],
  ['到期日过了', '停掉；什么都不删，日期往后一挪就回来'],
  ['流量 80% / 100%', '警告 / 暂停'],
  ['删除一份容器', '销毁；空间还回来'],
  ['解绑一台机器', '面板忘掉它；容器还在机器上跑'],
]" />

最后两条不一样，所以：要退掉一台机器，先把上面的容器删掉才是干净的做法。只解绑的话，那些容器
会继续跑在面板已经不认识的硬件上。

机器上软件的更新从它的页面上驱动；账号要更大的额度 —— 比套餐允许的容器数更多 —— 是发消息谈，
不是在线下单，具体看套餐页面。

---

## 8. 出问题的时候

| 你看到的 | 通常是什么 |
|---|---|
| 机器显示离线 | 它连不上面板。看机器是否开着、能不能往外联网；它不需要任何对外开放的端口。 |
| 创建被拒绝 | 机器在内存或 CPU 上放不下这个规格，并且会说清是哪一项。卖小一点，或者等负载降下来。 |
| 从市场创建失败，再建同名的又说名字已被占用 | 面板等下载系统等到超时就不等了，而机器其实把容器建完了，面板没记下来。先在**镜像**里把系统缓存到机器上，再选「来自这台主机」创建，那样不下载任何东西。机器上那份多出来的用 `hqnode rm <名字>` 清掉。 |
| 租户登不进去 | 他那份容器停了、到期了或因流量被暂停了 —— 也可能是云厂商的防火墙没放行 22。 |
| 租户的域名解析到了但没人回应 | 要么他容器里面没程序在听，要么云厂商的防火墙没把 80、443 放进这台机器。 |
| 所有租户的证书都申请不下来 | 机器上负责加密的那个门被挪离了常用端口。请求会在联系签发方之前就被拒掉。 |
| 容器里看到的是整台机器的内存 | **高级**里那一项没装。已经在跑的容器要重启之后才会看到自己的数字。 |
| 刚接进来就缺软件 | 每次启动它都会自己装；那台机器的页面会说哪一步失败了，以及装不上时该手动跑什么。 |

---

## 接下来

- [快速上手](quick-start.md) —— 你的租户按顺序要做的事。
- [它是怎么工作的](how-it-works.md) —— 用来跟别人解释这件事的短版本。
- [自己做镜像](/building-your-own-image)（英文）—— 提供一个不在已发布列表里的系统。
